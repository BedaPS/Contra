# Contra
Contra is an intelligent reconciliation pipeline designed to bridge the gap between unstructured financial communication and formal banking records. By leveraging OCR and fuzzy matching logic, Contra ingests payment proof from emails, validates them against real-time bank statements, and closes the loop by generating verified receipts.
